import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cam.longdino.LUNA',
  appName: 'L.U.N.A',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
